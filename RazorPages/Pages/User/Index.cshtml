@page
@model RazorPages.Pages.User.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserEntity[0].Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserEntity[0].Role)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.UserEntity) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Role)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-email="@item.Email">Edit</a> |
                <a asp-page="./Delete" asp-route-email="@item.Email">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
